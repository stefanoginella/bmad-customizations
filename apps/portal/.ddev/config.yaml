name: portal.woptimize
type: laravel
docroot: public
php_version: "8.4"
nodejs_version: "24"
webserver_type: nginx-fpm
# Matches the DDEV >= 1.25 prerequisite in README.md. Stops an older DDEV at
# `ddev start` instead of letting it fail later and confusingly.
ddev_version_constraint: ">= 1.25"
# Provisional. RunCloud provisioning (story 7) fixes the real major; this then mirrors it.
database:
  type: mariadb
  version: "11.8"

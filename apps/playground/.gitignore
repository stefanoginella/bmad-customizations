# WordPress core, installed by `ddev playground-setup`.
/wordpress/

# Dev tooling only — the suite never ships.
/vendor/
/.phpunit.cache/
/.phpunit.result.cache

# The previous-minor plugin, extracted by `ddev contract-suite` for one leg.
/.previous-connector/

---
topic: Woptimize monorepo structure, design-token pipeline, connector-portal contract, and deploy/release flows
updated: 2026-08-24T11:16
---

- (note) Source: _bmad-output/forge/monorepo-vs-multirepo/forged-idea.md (bmad-forge-idea, verdict HARDENED); rich input, express distill, no elicitation
- (decision) Slug repo-structure: forge folder names the question (monorepo-vs-multirepo), spec names the thing being specced
- (decision) One GitHub monorepo; separate repos rejected (private-package publishing or copy-drift for tokens, splits plugin-portal contract and local integration testing; access-boundary argument dead)
- (decision) Umami is deployment only, zero custom code: infra/umami holds compose + env, never a codebase
- (capability) CAP-1: all Woptimize code lives in the one monorepo (www theme+plugin, portal, playground, design-tokens, connector, umami infra, workflows); success = fresh clone gives working local dev
- (capability) CAP-2: one design-token source builds theme.json + stylesheet for WP and Tailwind preset + stylesheet for Laravel; success = one token edit changes both apps' built styles
- (capability) CAP-3: connector registers REST endpoints the portal calls with a site key, connector phones home, portal serves connector update zips; success = portal reaches client site, orphaned connector is pure no-ops
- (capability) CAP-4: per-app path-filtered GitHub Actions deploys; success = push touching one app deploys only that app
- (capability) CAP-5: www deploys rsync to releases/<sha> with symlink flip in wp-content; success = one-command rollback, no rebuild
- (capability) CAP-6: tag plugin-v* builds connector zip from its folder, portal serves it to client sites; success = tag makes new version available to sites
- (capability) CAP-7: content sync via custom DDEV provider prod.yaml (SSH + rsync, db and files, pull and push); success = routine pull refreshes local from prod
- (constraint) DDEV project root must be apps/www with docroot: wordpress, symlinks must be relative — else symlink targets fall outside the container mount
- (constraint) apps/www in git = custom theme + one site plugin only; full WP install gitignored in wordpress/
- (constraint) No raw RunCloud webhooks on the monorepo — every push would deploy everything
- (constraint) Karin's rule (amended): portal supports current + previous connector minor; patch releases never change the contract (hotfixes ship in any order); contract change requires minor/major bump, plugin ships first, portal after; no lockstep commits
- (constraint) Tomas's rule: design system = tokens + base styles first, shared components only when both apps need the same one — envelope, not truck
- (constraint) After launch content flows one way, production to local; ddev push on a live site destroys content edits
- (constraint) Rollback covers code only; DB and uploads roll back via backups
- (decision) No license system for the connector: offboarding = uninstall, orphaned plugin becomes pure no-ops
- (decision) Non-goals: separate repos, connector license system, Umami as maintained code, shared component library ahead of shared need
- (decision) Companions: repo-layout.md (directory catalog, apps/www layout, DDEV config) and release-and-deploy.md (deploy pipeline, release flows, versioning policy, content sync)
- (question) Input specifies www deploy mechanics but not portal deploy mechanics — what does the apps/portal deploy job do on the server?
- (question) Do connector integration tests against apps/playground run in CI, or is playground local-only?
- (assumption) Production servers are RunCloud-managed and reached over SSH from GitHub Actions (implied by the RunCloud-webhook rejection and rsync deploys)
- (event) Self-validate pass 1 (coherence): PASS — 7 capabilities each with intent+success; intents stay WHAT (mechanics pushed to release-and-deploy.md); all 7 constraints rule something out; 4 non-goals; success signal demonstrable; CAP-1..7 unique; prose lean
- (event) Self-validate pass 2 (preservation): PASS — every load-bearing claim from forged-idea.md landed in SPEC.md, repo-layout.md, or release-and-deploy.md. Wrapper-only content dropped: HARDENED verdict banner, section headings (Locked decisions / Rejected / Surviving risks framing)
- (decision) Assumption confirmed by Stefano: production is RunCloud-managed, GitHub Actions deploy over SSH — now a fact, recorded in release-and-deploy.md, dropped from assumptions
- (decision) Portal deploy mechanics: deliberately deferred to bmad-architecture (open question stays, marked deferred)
- (decision) Playground integration-test venue (CI vs local-only): deliberately deferred to bmad-architecture
- (event) Update self-validate: PASS both passes — assumption promoted to fact in release-and-deploy.md, assumptions section removed, two open questions retained as explicit architecture deferrals; no source claim lost
- (direction) Update (Stefano): adopt architecture spine architecture-woptimize.io-2026-08-24 as companion; spine supersedes spec text on conflict; CAP and AD IDs stay stable; four drifts to sync; AD rationale lives in the spine's .memlog.md
- (decision) Adopted companion: ../../planning-artifacts/architecture/architecture-woptimize.io-2026-08-24/ARCHITECTURE-SPINE.md — architect-owned, referenced not edited
- (decision) CAP-2 reworded: 'Tailwind preset' (v3 language) -> Tailwind 4 @theme CSS file + stylesheet for Laravel; intent unchanged (spine AD-3)
- (decision) Open question RESOLVED: portal deploys www-style — releases/<sha> + symlink flip, migrate --force before the flip, .env and storage/ in shared/ (spine AD-9); CAP-5 widened from www-only to both apps, ID kept
- (constraint) Portal migrations stay expand/contract — a symlink rollback is always safe without a DB restore (spine AD-10)
- (decision) Open question RESOLVED: connector integration suite runs local + CI from one DDEV definition, one command; the portal deploy job gates on a green suite in the same run (spine AD-11); both architecture deferrals removed from SPEC.md
- (decision) Content-sync push: pre-launch only with double confirmation — DDEV's own confirm + typed production hostname inside prod.yaml push commands; discipline rule, no hard block (spine content-sync convention)
- (decision) repo-layout corrected per spine amendment: apps/www/themes/woptimize-theme/ (plural, room for a child theme) + plugins/woptimize-core/; slugs fixed everywhere — theme woptimize-theme, site plugin woptimize-core
- (event) Update self-validate pass 1 (coherence): PASS — CAP-1..7 intact with intent+success, IDs stable; CAP-5 widened not renumbered; new constraints (expand/contract, double-confirm push) both rule something out; Open Questions section removed (empty); prose lean
- (event) Update self-validate pass 2 (preservation): PASS — all four spine drifts landed (CAP-2 @theme wording; portal deploy + test venue resolutions into release-and-deploy.md + constraints; double-confirmed push into CAP-7/constraint/release-and-deploy.md; themes-plural + woptimize-core into repo-layout.md); spine adopted in companions with supersession note; nothing load-bearing dropped
- (decision) Story Breakdown (Stefano): 11 stories agreed in execution order; spec_checkpoint on decision-heavy 4/6/7/8; done_checkpoint on production-touching 7/8/9/11; dispatch notes on 4/6/7/8/11
- (decision) Story 1: monorepo skeleton + www local dev — tree per repo-layout.md, apps/www DDEV docroot wordpress, gitignored WP install, themes/woptimize-theme + plugins/woptimize-core, relative per-item symlinks (CAP-1 www half)
- (decision) Story 2: portal scaffold — Laravel 13 in apps/portal, own DDEV, Pest + Pint wired (CAP-1 portal half)
- (decision) Story 3: design-token pipeline — DTCG source + Style Dictionary in packages/design-tokens, emits all four AD-3 artifacts via root tokens:build (CAP-2)
- (decision) Story 4: connector plugin + contract — skeleton, openapi.yaml both directions, woptimize/v1 endpoints, phone-home, AD-7 no-op discipline; spec_checkpoint; note: PHP 8.1 floor, no 8.2+ syntax (CAP-3 connector half)
- (decision) Story 5: portal contract side — site registry, onboarding + key issuance (AD-16), /api/connector/v1 endpoints, portal-to-site calls (CAP-3 portal half)
- (decision) Story 6: playground + integration suite — ddev playground-setup, one-command suite both venues, Karin matrix, AD-7 scenarios, reusable workflow (AD-11/18); spec_checkpoint; note: OpenAPI 3.1 validator caveat, 3.0.3 fallback
- (decision) Story 7: www deploy pipeline — www.yml path filters, RunCloud app setup, releases/<sha> + wp-content flip, one-command rollback (CAP-4/5); spec_checkpoint + done_checkpoint; note: AD-12 panel-or-pipeline
- (decision) Story 8: portal deploy pipeline — portal.yml needs green suite, releases + current flip, shared/, migrate --force before flip (CAP-4/5, AD-9/10); spec_checkpoint + done_checkpoint; note: prove web root current/public on the real panel
- (decision) Story 9: connector release delivery — plugin-v* tag, connector.yml zip to shared/storage/connector-releases, portal update-check + zip endpoints, Update URI integration (CAP-6, AD-17); done_checkpoint
- (decision) Story 10: content sync provider — prod.yaml routine pull + pre-launch-only double-confirmed push (CAP-7)
- (decision) Story 11: umami + ops floor — infra/umami compose + umami.yml + pg_dump cron, RunCloud backups, uptime checks (AD-12/14/19); done_checkpoint; note: panel-or-pipeline, pg_dump into RunCloud-backed folder
- (event) stories.yaml schema check: PASS — 11 entries parse, ids 1..11 unique quoted prefix-free, no status field, required fields present; checkpoints and dispatch notes per Stefano's choices

---
scope: WOptimize monorepo: www (WP), portal (Laravel), connector plugin, design tokens, playground, umami infra, CI/deploy
purpose: build-substrate
altitude: initiative
updated: 2026-08-24T11:00
---

- (direction) Coaching path; deliverable = spine only; greenfield (no code exists yet); driving input = SPEC-repo-structure + companions repo-layout.md and release-and-deploy.md + forged-idea.md
- (constraint) Settled by SPEC-repo-structure (inherited, not re-decided): one monorepo per repo-layout.md; token pipeline builds theme.json+stylesheet (WP) and Tailwind preset+stylesheet (Laravel); connector-portal two-way REST with site key, no license system; per-app path-filtered Actions deploys, no raw RunCloud webhooks; www releases/<sha>+symlink flip; plugin-v* tag releases; Karin's rule current+previous minor; DDEV root apps/www docroot wordpress relative symlinks; content one-way prod->local after launch
- (question) Q1 (from spec): portal deploy mechanics on the server — www-style releases+symlink flip or in-place?
- (question) Q2 (from spec): connector integration tests against apps/playground — CI or local-only?
- (decision) Q1 RESOLVED: portal deploys = www-style releases/<sha> + symlink flip (Laravel zero-downtime). CI builds (composer + assets), rsyncs to releases/<sha>; .env and storage/ live in shared/, symlinked per release; migrate --force runs before the current-symlink flip; RunCloud web root = current/public. Rollback flips code only. Binds: portal deploy job. Prevents: two deploy models in one repo / no portal rollback.
- (decision) New discipline rule (AD candidate): portal DB migrations must stay compatible with the previous release (expand/contract) so a symlink rollback is always safe — same shape as Karin's rule. Prevents: a migration that strands rollback.
- (decision) Q2 RESOLVED: connector integration tests run local + CI with ONE definition. Single command (ddev exec ...) runs the suite; CI uses official ddev/github-action-setup-ddev to reuse the same .ddev config (verified on web 2026-08-24). Trigger: packages/connector + apps/portal paths and plugin-v* tags only — never www pushes. Suite includes Karin's-rule matrix: portal tested against connector current AND previous minor. Est. job time 5-8 min. Prevents: unguarded contract drift; a second CI-only test env that drifts from local.
- (version) Web-verified 2026-08-24: style-dictionary 5.5.2 (npm, active, DTCG-native); Tailwind CSS 4.3.3 (Jul 2026, CSS-first @theme; 'preset' is v3 language); DTCG $value/$type token format is the de facto standard; ddev/github-action-setup-ddev is the official DDEV CI action
- (decision) Token pipeline: source = DTCG JSON ($value/$type) in packages/design-tokens; build tool = Style Dictionary 5.5.2 with custom formats. Emits: theme.json + tokens stylesheet for WP, CSS @theme file + stylesheet for Laravel (Tailwind 4.3 CSS-first). Prevents: two token dialects / hand-rolled build script ownership.
- (decision) SPEC amendment to propose at finalize: CAP-2 wording 'Tailwind preset' (v3 language) -> 'Tailwind @theme CSS file' (v4 mechanics). Intent unchanged.
- (decision) Built style artifacts are GITIGNORED and built on demand (Option B). One command in packages/design-tokens writes every artifact directly to its consumer's canonical path (e.g. theme.json at WP theme root); consumers never hand-copy. Per-app setup runs it once locally; www and portal deploy jobs run it before rsync. Prevents: hand-edited generated files; www/portal reading styles from different places.
- (decision) Contract conventions (CAP-3): (1) all connector REST endpoints under woptimize/v1, additive-only within v1; Karin's rule tracks connector minor, namespace v2 = major event. (2) site key in X-Woptimize-Site-Key header both directions, connector compares with hash_equals(). (3) portal owns the site registry (url, key, connector version, last-seen); connector owns only its key+settings in wp_options — one writer per fact. (4) connector reports its version in every phone-home and every response. (5) hard no-op invariant: any remote failure (bad key, portal gone, offboarded) degrades to silent no-op; the connector never breaks a client site.
- (decision) Contract source of truth: packages/connector/openapi.yaml, OpenAPI 3.1 (3.2 is current but PHP tooling targets 3.0/3.1 — verified 2026-08-24). Contract-first: a contract change edits the YAML before code. CI integration suite validates real responses against the file (validator lib picked at build time, e.g. league/openapi-psr7-validator). No codegen. Prevents: portal and connector implementing different shapes; silent contract drift.
- (decision) Phone-home transport: WP-Cron daily + ping on activation and on self-update. Cadence is config, not architecture — adjustable anytime.
- (decision) Environments: prod + local only, NO staging. Deferred with revisit triggers: first migration that scares, or portal client-data risk grows. Safety net = symlink rollback (both apps) + CI gate on connector.
- (decision) Topology: ONE Netcup root VPS, RunCloud already installed. www + portal as RunCloud web apps; Umami via docker-compose beside RunCloud on the same box (infra/umami). Server config principle: RunCloud panel or the deploy pipeline — never ad-hoc SSH; Umami/docker is the contained exception, fully described in infra/umami.
- (decision) Secrets: GitHub FREE private repo — no Actions environments available; use repo-level secrets with app prefixes (WWW_, PORTAL_, UMAMI_). Runtime secrets (.env, WP salts) live only on the server in shared/ — never in repo or CI logs.
- (constraint) User facts: Netcup root VPS exists, RunCloud set up, apps not yet created; user prefers panel configuration over direct SSH where possible (replicability); www additionally backed up by WP Umbrella.
- (decision) Backups (P-2 resolved): RunCloud Backup (app-agnostic, files+db, offsite destination) covers www and portal; WP Umbrella = extra layer on www; Umami Postgres is INVISIBLE to RunCloud (inside Docker) -> pg_dump cron in infra/umami writes into a RunCloud-backed folder. Prevents: analytics data with zero backups; rollback constraint (DB/uploads via backups) with no owner.
- (version) Web-verified 2026-08-24: Laravel 13 (Mar 2026, current; PHP 8.3-8.5) recommended PHP 8.4; WordPress 7.1 (Aug 19 2026); Node.js 24 = Active LTS (26 goes LTS Oct 2026); RunCloud Backup app-agnostic files+db confirmed
- (decision) RATIFIED Topic 6: paradigm = 'multi-app monorepo: build-time sharing, runtime independence' — apps deploy/fail independently; packages are build-time only; sole runtime coupling = woptimize/v1 contract; dependency direction apps->packages, never app->app, never package->app.
- (decision) RATIFIED stack pins: PHP 8.4, Laravel 13, WordPress 7.1, Node 24 LTS (bump to 26 when LTS in Oct 2026), Tailwind 4.3, Style Dictionary 5.5, MariaDB as RunCloud-provisioned with DDEV mirroring major version, OpenAPI 3.1.
- (decision) RATIFIED test/style conventions: portal = Pest + Laravel Pint; WP side (theme, site plugin, connector) = PHPUnit + WordPress Coding Standards via PHPCS; integration suite one command via ddev exec, same local and CI; each app keeps its own framework idioms — no shared conventions across the WP/Laravel border beyond tokens + contract.
- (event) Coaching complete; all 6 topics resolved; entering Finalize (distill -> reconcile -> reviewer gate -> triage -> close)
- (decision) Content-sync push guard (user request): prod.yaml push commands carry a two-layer guard. Layer 1 pre-launch: DDEV's own confirm + a typed-hostname confirmation inside the provider's push commands. Layer 2 post-launch: a committed LAUNCHED marker makes the push guard exit with an error unconditionally — 'push is pre-launch only' becomes mechanical, not memory. Prevents: ddev push destroying live content.
- (decision) Reconcile findings to fix in one pass: (1) BLOCKER AD-9 www flip location = inside wp-content/ (theme+plugin symlinks), distinct from portal web-root current/ flip — fix rule + diagram; (2) restore prod.yaml push capability (pre-launch) + guard design; (3) AD-3 add token scope enumeration fonts/colors/typography/spacing/motion/buttons/links; (4) AD-7 carry no-license-system non-goal (update endpoint performs no license validation).
- (decision) AMENDED by Stefano: NO hard block / no LAUNCHED marker. Push guard = double confirmation only (DDEV's own confirm + typed production hostname inside prod.yaml push commands). 'Push is pre-launch only' remains a discipline rule, not mechanically enforced.
- (decision) Connector client-site floor (user): PHP >= 8.1 / WP >= 6.7. Connector code uses no PHP 8.2+ syntax; CI connector leg runs at 8.1; playground installs WP at the floor. Note: PHP 8.1 is EOL (security fixes ended Dec 2025) — client-side constraint, not our posture.
- (decision) Reviewer-gate fixes applied in one pass: reconcile R1-R4 (www flip in wp-content + fixed slugs woptimize/woptimize-site, prod.yaml push restored w/ double-confirm, token scope enumerated, no-license carried into AD-7); rubric F-1..F-9 (AD-19 observability floor, AD-17 zip handoff, floor Stack row, AD-4 both directions, AD-11 no-op scenarios, Node/Composer layout convention, MariaDB/DDEV pin notes, umami.yml added, playground own .ddev); web-verify V1-V2 (validator 3.0.x caveat + 3.0.3 fallback in Deferred, PHP 8.5 bump deferred); adversarial C1-C3 (portal-side contract governed+validated, AD-16 key lifecycle, AD-17 update delivery w/ fixed slug+zip anatomy+Update URI mechanism), H1-H8 (theme.base.json merge, named artifact paths + tokens-first build order + root tokens:build, path filters include consumed packages, suite as reusable workflow gating portal deploy, AD-18 playground contract, www release anatomy, WP-core/Laravel error envelopes + version header, onboarding-only registry rows + permanent-quiet 4xx), M1-M5 (N-1 leg vs tagged YAML, connector-reported rest_url stored, umami pipeline, skip-and-report first release, key format+storage+manual rotation), L1-L5 (supersession note, playground mirrors www rules, per-surface workflow naming, secrets naming table, per-unit package.json + root script).
- (assumption) RunCloud web root -> current/public for the portal is verified-plausible (free-form web root + RunCloud's own symlink mechanics) but not documented end-to-end as one recipe; the first portal deploy story must prove it on the real panel.
- (event) spine finalized — lint clean (0 findings), all reviewer-gate findings applied, status: final
- (decision) UPDATE (Stefano): www slugs corrected — site plugin slug woptimize-core (was woptimize-site), theme slug woptimize-theme (was woptimize). Layout: apps/www/themes/<theme-slug>/ (plural) instead of apps/www/theme/ — leaves room for a future child theme beside it. Symlinks stay per-item (wp-content/themes/woptimize-theme, wp-content/plugins/woptimize-core), same shape local and prod. Amends AD-3 artifact paths, AD-9 flip targets, Structural Seed. Adds a 4th spec-drift item: repo-layout.md says 'theme/' singular.
- (event) spine amended (slugs + themes/ layout), lint clean, status stays final

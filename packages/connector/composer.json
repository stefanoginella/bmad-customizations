{
    "name": "woptimize/connector",
    "description": "The WOptimize connector plugin. Dev tooling only — the plugin itself has no Composer runtime dependencies and ships without vendor/.",
    "license": "GPL-2.0-or-later",
    "require": {
        "php": ">=8.1"
    },
    "require-dev": {
        "brain/monkey": "^2.7",
        "dealerdirect/phpcodesniffer-composer-installer": "^1.2",
        "phpcompatibility/phpcompatibility-wp": "^2.1",
        "phpunit/phpunit": "^10.5",
        "squizlabs/php_codesniffer": "^3.13",
        "symfony/yaml": "^6.4",
        "wp-coding-standards/wpcs": "^3.4"
    },
    "config": {
        "allow-plugins": {
            "dealerdirect/phpcodesniffer-composer-installer": true
        },
        "platform": {
            "php": "8.1.0"
        },
        "sort-packages": true
    },
    "scripts": {
        "test": "phpunit",
        "lint": "phpcs",
        "lint:fix": "phpcbf"
    },
    "scripts-descriptions": {
        "test": "Run the Brain Monkey unit suite.",
        "lint": "Check WordPress Coding Standards and PHP 8.1+ compatibility.",
        "lint:fix": "Fix what PHPCS can fix on its own."
    }
}

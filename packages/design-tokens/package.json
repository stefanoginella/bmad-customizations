{
    "$schema": "https://www.schemastore.org/package.json",
    "name": "@woptimize/design-tokens",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "description": "The one design-token source for woptimize.io. Builds the four AD-3 style artifacts.",
    "engines": {
        "node": ">=24"
    },
    "scripts": {
        "build": "node build.js",
        "test": "node --test"
    },
    "devDependencies": {
        "style-dictionary": "^5.5.2"
    }
}

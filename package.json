{
    "$schema": "https://www.schemastore.org/package.json",
    "name": "woptimize",
    "private": true,
    "description": "Root scripts only. There are no workspaces and no root dependencies — every unit installs its own.",
    "engines": {
        "node": ">=24"
    },
    "scripts": {
        "tokens:build": "npm --prefix packages/design-tokens install --no-audit --no-fund && npm --prefix packages/design-tokens run build",
        "tokens:test": "npm run tokens:build && npm --prefix packages/design-tokens run test"
    }
}
